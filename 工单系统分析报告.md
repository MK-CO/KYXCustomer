# 工单系统分析报告

## 一、业务需求概述

### 1. 售后工单时效标准
- **首次回复**：2分钟内
- **紧急工单**：8分钟要给解决方案
  - 若备注正在联系沟通的，需提供正在联系的截图（微信/电话）后，可按照普通时效处理
  - 提供不出来可打差评
- **普通工单**：15分钟要给解决方案
  - 若备注正在联系沟通的，可以要一下正在联系的截图（微信/电话）
  - 提供不出来打差评

### 2. 派单及地址库工单时效标准
- **接单**：3分钟内
- **紧急工单**：10分钟回复进度/结果
- **普通工单**：30分钟回复进度/结果

---

## 二、工单分类体系

### 系统中的工单种类

根据代码分析，系统主要包含以下工单分类：

#### 1. 售后类工单（41个子类型）
主要用于处理订单售后问题。

**核心类型**：
- `AFTER_SALE` - 售后（主类别）
- `AFTER_SALE_PAY` - 售后赔付
- `BUBBLE` - 气泡/划痕/杂质/裁剪不齐（level=2）
- `REPAIR_CHU_JIU` - 补除旧（level=3，紧急）
- `SIZE_NOT_ENOUGH` - 尺寸不够（level=2）
- `INSTALL_NOT_PLACE` - 安装不到位（level=2）
- `MUTUAL_MACHINES` - 互发机器（level=2）
- `UN_HEMMED` - 未包边（level=2）
- `DUST` - 灰尘/气泡（level=2）
- `AFTER_SALES_PRODUCTS` - 产品原因二次售后（level=2）
- `PRYING_ORDERS` - 评价产品/撬单（level=1）
- `CAR_DAMAGE` - 车损（level=1）
- `NEGATIVE_REVIEW` - 中差评（level=1）
- `COMPLAINT` - 投诉（level=1）
- `RETURN_GOODS` - 退货
- `PRODUCT_FEEDBACK` - 产品反馈
- `ORDER_FEEDBACK` - 订单反馈

#### 2. 派单类工单（5个子类型）
用于订单派单流程控制。

- `URGE_INSTALL` - 催安装
- `EXCHANGE` - 换店（level=2）
- `REFUSE` - 拒单（level=2）
- `REVOKE` - 撤单
- `CHECK_PRICE` - 核实价格（level=2）

#### 3. 财务类工单（2个子类型）
- `URGE_PAYMENT` - 催打款
- `AFTER_SALE_PAY` - 售后赔付

#### 4. 找点工单（地址库工单）
- `FIND_SHOP` - 找点

#### 5. 门店编辑工单（6个子类型）
- `EDIT_SHOP` - 门店编辑
- `BOND_RETURN` - 保证金退回
- `BOND_PAYMENT` - 保证金缴纳
- `REDUCED_SHOP` - 门店降价
- `VERIFY_SHOP` - 核实门店
- `DELETE_SHOP` - 删除门店
- `FINISH_SHOP` - 门店已核实

#### 6. 其他工单类型
- `OUTLETS_COMPLAINT` - 门店投诉工单
- `URGE_HF` - 催回访
- `ADD_WECHAT` - 加微信
- `ORDER_AUDIT` - 订单审核
- `SETTLEMENT_OF_VERIFY` - 结款核实
- `FILL_ORDER` - 录补款单
- `SHOP_NO_ORDER` - 门店无订单
- `SHOP_FEED_BACK` - 门店反馈
- `CHECK_SELLER` - 核实卖家
- `INVOICE` - 开发票
- `COMMON` - 通用工单

---

## 三、数据库表结构

### 核心表

#### 1. `t_work` - 工单基础表
所有工单都会在此表创建记录。

**关键字段**：
```sql
- id: 工单ID
- type: 工单类型（WorkTypeEnum枚举值）
- level: 紧急程度（1=普通, 2=中等, 3=紧急）
- state: 工单状态（WorkStateEnum枚举值）
- createUid/createName/createType: 发起人信息
- orderUid/orderName/orderType: 接单人信息
- orderTime: 接单时间
- remark: 完结备注
- endTime: 完结时间
- hintStatus: 提示状态（1=暂不联系, 2=稍后联系, 3=车损中差评, 0=不设置）
- formKey: 关联表单
- relaunch: 重新发起标记
```

**实体类路径**：
`order-order/src/main/java/com/kyx/order/domain/Work.java`

**Mapper路径**：
- `order-order/src/main/java/com/kyx/order/mapper/WorkMapper.java`
- `order-order/src/main/resources/mapper/order/WorkMapper.xml`

#### 2. `t_work_order` - 售后工单扩展表
售后类工单的详细信息。

**关键字段**：
```sql
- id: 主键
- workId: 关联t_work.id
- sUid: 卖家id
- orderNo: 订单编号
- content: 工单内容
- image: 工单图片
- reissue: 补发标记
- rate: 评分（1-5星）
- timeoutDate: 超时日期
- timeoutNum: 超时次数
- reviewStatus/reviewRemark/reviewId: 审核信息
```

**实体类路径**：
`order-order/src/main/java/com/kyx/order/domain/WorkOrder.java`

**Mapper路径**：
- `order-order/src/main/java/com/kyx/order/mapper/WorkOrderMapper.java`
- `order-order/src/main/resources/mapper/order/WorkOrderMapper.xml`

#### 3. `t_work_order_problem` - 工单问题记录表
记录售后工单的问题处理记录。

**实体类路径**：
`order-order/src/main/java/com/kyx/order/domain/WorkOrderProblem.java`

**Mapper路径**：
- `order-order/src/main/java/com/kyx/order/mapper/WorkOrderProblemMapper.java`
- `order-order/src/main/resources/mapper/order/WorkProblemMapper.xml`

### 分类工单扩展表

#### 4. `t_work_find_shop` - 找点工单表
**关键字段**：
```sql
- id: 主键
- orderId: 关联订单
- orderNo: 订单编号
- finished: 完成状态
- region: 区域
- regionPath: 区域路径
- projectCar: 施工车型
- projectName: 施工项目
- content: 备注
- blindness: 是否盲点（0=盲点, 1=有门店）
- upgradeState: 升级状态
```

**实体类路径**：
`order-order/src/main/java/com/kyx/order/domain/WorkFindShop.java`

**Mapper路径**：
- `order-order/src/main/java/com/kyx/order/mapper/WorkFindShopMapper.java`
- `order-order/src/main/resources/mapper/order/WorkFindShopMapper.xml`

#### 5. `t_work_shop_edit` - 门店编辑工单表
**关键字段**：
```sql
- id: 主键
- outletsId: 网点ID
- shopId: 门店ID
- content: 编辑备注
- reason: 删除原因
- assignName: 指派人
- image: 图片
```

**实体类路径**：
`order-order/src/main/java/com/kyx/order/domain/WorkShopEdit.java`

**Mapper路径**：
- `order-order/src/main/java/com/kyx/order/mapper/WorkShopEditMapper.java`
- `order-order/src/main/resources/mapper/order/WorkShopEditMapper.xml`

#### 6. `pending_work_order` - 待发送工单表
用于工单待处理队列。

**实体类路径**：
`order-system/src/main/java/com/kyx/system/domain/PendingWorkOrder.java`

**Mapper路径**：
`order-system/src/main/resources/mapper/system/PendingWorkOrderMapper.xml`

#### 7. 其他扩展表
- `t_work_common` - 通用工单表（WorkCommon）
- `t_work_complaint` - 投诉工单表（WorkComplaint）
- `t_order_after_sale` - 订单售后表（OrderAfterSale）

---

## 四、枚举类定义

### 1. 工单类型枚举 - WorkTypeEnum

**文件路径**：
`order-order/src/main/java/com/kyx/order/enums/WorkTypeEnum.java`

**枚举结构**：
```java
WorkTypeEnum(String name, Integer level, String formKey, Class<? extends WorkExecute> execute)
```

**关键判断方法**：

#### (1) 售后类型判断
```java
// 返回所有售后类工单类型
public static List<WorkTypeEnum> afterSaleTypes()

// 返回类型包括：
AFTER_SALE, AFTER_SALE_PAY, BUBBLE, REPAIR_CHU_JIU,
SIZE_NOT_ENOUGH, INSTALL_NOT_PLACE, MUTUAL_MACHINES,
UN_HEMMED, DUST, AFTER_SALES_PRODUCTS, PRYING_ORDERS,
CAR_DAMAGE, NEGATIVE_REVIEW, COMPLAINT 等41个
```

#### (2) 派单类型判断
```java
// 判断是否为派单类工单
public static boolean isPdType(WorkTypeEnum type)

// 包括类型：
URGE_INSTALL, EXCHANGE, REFUSE, REVOKE, CHECK_PRICE
```

#### (3) 财务类型判断
```java
// 判断是否为财务类工单
public static boolean isCwType(WorkTypeEnum type)

// 包括类型：
URGE_PAYMENT, AFTER_SALE_PAY
```

#### (4) 门店编辑类型判断
```java
// 判断是否为门店编辑类工单
public static boolean isEditShopType(WorkTypeEnum type)

// 判断是否为核实类工单
public static boolean isVerifyType(WorkTypeEnum type)

// 包括类型：
EDIT_SHOP, BOND_RETURN, BOND_PAYMENT, REDUCED_SHOP,
VERIFY_SHOP, DELETE_SHOP, FINISH_SHOP
```

#### (5) 卖家工单判断
```java
// 返回卖家可见的工单类型
public static List<WorkTypeEnum> sellerTypes()
public static List<WorkTypeEnum> sellerNewTypes()

// 返回22个卖家可操作的工单类型
```

### 2. 工单状态枚举 - WorkStateEnum

**文件路径**：
`order-order/src/main/java/com/kyx/order/enums/WorkStateEnum.java`

```java
public enum WorkStateEnum {
    WAIT("待处理"),          // 初始状态
    RECEIVED("已接单"),      // 已接单
    PROCESSING("处理中"),    // 处理中
    FINISH("已完结"),        // 已完结
    COMMENT("已评价"),       // 已评价
    UNDONE("未完成")         // 未完成
}
```

### 3. 工单超时配置枚举 - WorkTimeOutType

**文件路径**：
`order-order/src/main/java/com/kyx/order/enums/WorkTimeOutType.java`

**枚举结构**：
```java
WorkTimeOutType(int normal, int middle, int urgent)
// normal: 普通工单超时时间（分钟）
// middle: 中等工单超时时间（分钟）
// urgent: 紧急工单超时时间（分钟）
```

**配置示例**：
```java
// 紧急工单（level=3）
REPAIR_CHU_JIU(1440, 1440, 1440)        // 1440分钟 = 24小时

// 中等工单（level=2）
BUBBLE(7200, 7200, 7200)                // 7200分钟 = 5天
SIZE_NOT_ENOUGH(7200, 7200, 7200)
INSTALL_NOT_PLACE(7200, 7200, 7200)
MUTUAL_MACHINES(7200, 7200, 7200)
UN_HEMMED(7200, 7200, 7200)
DUST(7200, 7200, 7200)
AFTER_SALES_PRODUCTS(7200, 7200, 7200)
EXCHANGE(7200, 7200, 7200)

// 普通工单（level=1）
PRYING_ORDERS(1440, 1440, 1440)         // 1440分钟 = 24小时
CAR_DAMAGE(1440, 1440, 1440)
NEGATIVE_REVIEW(1440, 1440, 1440)
COMPLAINT(1440, 1440, 1440)
```

**注意**：其他工单类型不设置超时配置。

---

## 五、工单类型判断逻辑

### 判断依据总结

| 工单分类 | 判断字段 | 判断方法 | 代码位置 |
|---------|---------|---------|---------|
| **售后工单** | `t_work.type` | `WorkTypeEnum.afterSaleTypes()` | WorkTypeEnum.java |
| **派单工单** | `t_work.type` | `WorkTypeEnum.isPdType(type)` | WorkTypeEnum.java |
| **财务工单** | `t_work.type` | `WorkTypeEnum.isCwType(type)` | WorkTypeEnum.java |
| **找点工单** | `t_work.type` | `type == FIND_SHOP` | WorkTypeEnum.java |
| **门店编辑工单** | `t_work.type` | `WorkTypeEnum.isEditShopType(type)` | WorkTypeEnum.java |
| **紧急程度** | `t_work.level` | `1=普通, 2=中等, 3=紧急` | Work.java |
| **工单状态** | `t_work.state` | `WorkStateEnum` 枚举值 | WorkStateEnum.java |
| **超时判断** | `t_work_order.timeoutDate` | 超时日期字段 | WorkOrder.java |

### 实际判断代码示例

```java
// 1. 判断工单是售后还是派单
if (WorkTypeEnum.afterSaleTypes().contains(work.getType())) {
    // 售后工单逻辑
    // 紧急(level=3): 8分钟给方案
    // 普通(level=1/2): 15分钟给方案

    if (work.getLevel() == 3) {
        // 紧急售后工单：8分钟内必须给解决方案
    } else {
        // 普通售后工单：15分钟内必须给解决方案
    }

} else if (WorkTypeEnum.isPdType(work.getType())) {
    // 派单工单逻辑
    // 接单: 3分钟内
    // 紧急(level=3): 10分钟回复进度
    // 普通(level=1): 30分钟回复进度

    if (work.getState() == WorkStateEnum.WAIT) {
        // 待接单状态：3分钟内必须接单
    } else if (work.getLevel() == 3) {
        // 紧急派单：10分钟内回复进度
    } else {
        // 普通派单：30分钟内回复进度
    }
}

// 2. 判断紧急程度
switch (work.getLevel()) {
    case 3:
        // 紧急工单
        break;
    case 2:
        // 中等工单
        break;
    case 1:
    default:
        // 普通工单
        break;
}

// 3. 判断工单状态
switch (work.getState()) {
    case WAIT:
        // 待处理（刚创建）
        break;
    case RECEIVED:
        // 已接单
        break;
    case PROCESSING:
        // 处理中
        break;
    case FINISH:
        // 已完结
        break;
    case COMMENT:
        // 已评价
        break;
    case UNDONE:
        // 未完成
        break;
}
```

---

## 六、工单执行器（Execute Handler）

### 执行器分类

系统采用**策略模式**，根据工单类型动态加载不同的执行器。

| 执行器类 | 处理的工单类型 | 文件路径 |
|---------|--------------|---------|
| **WorkOrderExecuteImpl** | 售后类工单（最复杂，83KB） | order-order/src/main/java/com/kyx/order/execute/impl/WorkOrderExecuteImpl.java |
| **WorkAfterSaleExecuteImpl** | 售后类别处理 | order-order/src/main/java/com/kyx/order/execute/impl/WorkAfterSaleExecuteImpl.java |
| **WorkFindShopExecuteImpl** | 找点工单（FIND_SHOP） | order-order/src/main/java/com/kyx/order/execute/impl/WorkFindShopExecuteImpl.java |
| **WorkShopEditExecuteImpl** | 门店编辑工单 | order-order/src/main/java/com/kyx/order/execute/impl/WorkShopEditExecuteImpl.java |
| **WorkComplaintExecuteImpl** | 投诉工单（OUTLETS_COMPLAINT） | order-order/src/main/java/com/kyx/order/execute/impl/WorkComplaintExecuteImpl.java |
| **WorkCommonExecuteImpl** | 通用工单（COMMON） | order-order/src/main/java/com/kyx/order/execute/impl/WorkCommonExecuteImpl.java |

### 执行器加载机制

在 `WorkTypeEnum` 枚举中，每个工单类型都指定了对应的执行器：

```java
// 示例
FIND_SHOP("找点", null, "work_find_shop_form", WorkFindShopExecuteImpl.class)
EDIT_SHOP("门店编辑", null, "work_shop_edit_form", WorkShopEditExecuteImpl.class)
COMMON("通用工单", null, "work_common_form", WorkCommonExecuteImpl.class)
```

系统会根据 `work.getType().getExecute()` 动态实例化对应的执行器处理工单逻辑。

---

## 七、工单时效监控

### 定时任务

**文件路径**：
`order-admin-api/src/main/java/com/kyx/task/WorkOrderTask.java`

该定时任务会定期扫描工单表，检查超时情况：
- 检查 `t_work_order.timeoutDate` 字段
- 根据 `WorkTimeOutType` 配置的超时时间计算
- 更新 `timeoutNum` 超时次数

### 时效标准映射

| 业务要求 | 系统字段 | 判断依据 |
|---------|---------|---------|
| **售后-首次回复：2分钟** | 未配置自动超时 | 需人工监控或新增定时任务 |
| **售后-紧急：8分钟给方案** | `level=3` | 通过 `WorkTypeEnum.level` 判断 |
| **售后-普通：15分钟给方案** | `level=1/2` | 通过 `WorkTypeEnum.level` 判断 |
| **派单-接单：3分钟** | `orderTime` | `t_work.orderTime` 字段记录 |
| **派单-紧急：10分钟回复** | `level=3` | 通过 `WorkTypeEnum.level` 判断 |
| **派单-普通：30分钟回复** | `level=1` | 通过 `WorkTypeEnum.level` 判断 |

### 超时检查逻辑（伪代码）

```java
// 售后工单超时检查
if (WorkTypeEnum.afterSaleTypes().contains(work.getType())) {
    // 首次回复超时检查（2分钟）
    if (work.getState() == WorkStateEnum.WAIT) {
        long waitMinutes = (now - work.getCreateTime()) / 60000;
        if (waitMinutes > 2) {
            // 超时：首次回复超过2分钟
        }
    }

    // 解决方案超时检查
    if (work.getState() == WorkStateEnum.RECEIVED || work.getState() == WorkStateEnum.PROCESSING) {
        long processMinutes = (now - work.getOrderTime()) / 60000;

        if (work.getLevel() == 3 && processMinutes > 8) {
            // 超时：紧急工单超过8分钟未给方案
        } else if (work.getLevel() <= 2 && processMinutes > 15) {
            // 超时：普通工单超过15分钟未给方案
        }
    }
}

// 派单工单超时检查
if (WorkTypeEnum.isPdType(work.getType())) {
    // 接单超时检查（3分钟）
    if (work.getState() == WorkStateEnum.WAIT) {
        long waitMinutes = (now - work.getCreateTime()) / 60000;
        if (waitMinutes > 3) {
            // 超时：接单超过3分钟
        }
    }

    // 回复进度超时检查
    if (work.getState() == WorkStateEnum.RECEIVED || work.getState() == WorkStateEnum.PROCESSING) {
        long processMinutes = (now - work.getOrderTime()) / 60000;

        if (work.getLevel() == 3 && processMinutes > 10) {
            // 超时：紧急派单超过10分钟未回复进度
        } else if (work.getLevel() <= 2 && processMinutes > 30) {
            // 超时：普通派单超过30分钟未回复进度
        }
    }
}
```

---

## 八、服务和控制器

### 服务接口和实现

| 服务接口 | 实现类 | 说明 |
|---------|-------|------|
| `IWorkService` | `WorkServiceImpl` | 工单基础服务 |
| `IWorkOrderService` | `WorkOrderServiceImpl` | 售后工单服务 |
| `IWorkOrderProblemService` | `WorkOrderProblemServiceImpl` | 工单问题记录服务 |
| `IPendingWorkOrderService` | `PendingWorkOrderServiceImpl` | 待处理工单服务 |
| `IWorkFindShopService` | `WorkFindShopServiceImpl` | 找点工单服务 |
| `IWorkShopEditService` | `WorkShopEditServiceImpl` | 门店编辑工单服务 |

**服务实现路径**：
- `order-order/src/main/java/com/kyx/order/service/`
- `order-order/src/main/java/com/kyx/order/service/impl/`
- `order-system/src/main/java/com/kyx/system/service/`

### 控制器入口

| 控制器 | 说明 | 文件路径 |
|-------|------|---------|
| `WorkController` | 工单主控制器 | order-admin-api/src/main/java/com/kyx/admin/controller/WorkController.java |
| `WorkOrderController` | 售后工单控制器 | order-admin-api/src/main/java/com/kyx/admin/controller/WorkOrderController.java |
| `WorkOrderProblemController` | 工单问题控制器 | order-admin-api/src/main/java/com/kyx/admin/controller/WorkOrderProblemController.java |

**主要接口**：
- `POST /work/create` - 创建工单
- `POST /work/receive` - 接单
- `POST /work/process` - 处理工单
- `POST /work/finish` - 完结工单
- `GET /work/list` - 工单列表查询
- `GET /work/detail` - 工单详情

---

## 九、工单流程图

### 1. 售后工单流程

```
创建工单（WAIT）
    ↓
接单（RECEIVED）← 超时检查：3分钟内接单
    ↓
处理中（PROCESSING）← 超时检查：
    ↓                 - 紧急(level=3): 8分钟给方案
    ↓                 - 普通(level≤2): 15分钟给方案
已完结（FINISH）
    ↓
已评价（COMMENT）
```

### 2. 派单工单流程

```
创建工单（WAIT）
    ↓
接单（RECEIVED）← 超时检查：3分钟内接单
    ↓
处理中（PROCESSING）← 超时检查：
    ↓                 - 紧急(level=3): 10分钟回复进度
    ↓                 - 普通(level≤2): 30分钟回复进度
已完结（FINISH）
```

---

## 十、核心代码文件清单

### 实体类（Domain）
```
order-order/src/main/java/com/kyx/order/domain/
├── Work.java                    # 工单基础实体
├── WorkOrder.java               # 售后工单实体
├── WorkOrderProblem.java        # 工单问题记录
├── WorkFindShop.java            # 找点工单
├── WorkShopEdit.java            # 门店编辑工单
├── WorkCommon.java              # 通用工单
├── WorkComplaint.java           # 投诉工单
└── OrderAfterSale.java          # 订单售后

order-system/src/main/java/com/kyx/system/domain/
└── PendingWorkOrder.java        # 待处理工单
```

### 枚举类（Enums）
```
order-order/src/main/java/com/kyx/order/enums/
├── WorkTypeEnum.java            # 工单类型枚举（核心）
├── WorkStateEnum.java           # 工单状态枚举
└── WorkTimeOutType.java         # 工单超时配置枚举
```

### Mapper接口
```
order-order/src/main/java/com/kyx/order/mapper/
├── WorkMapper.java
├── WorkOrderMapper.java
├── WorkOrderProblemMapper.java
├── WorkFindShopMapper.java
└── WorkShopEditMapper.java
```

### Mapper XML
```
order-order/src/main/resources/mapper/order/
├── WorkMapper.xml
├── WorkOrderMapper.xml
├── WorkOrderProblemMapper.xml
├── WorkFindShopMapper.xml
├── WorkShopEditMapper.xml
└── WorkProblemMapper.xml

order-system/src/main/resources/mapper/system/
└── PendingWorkOrderMapper.xml
```

### 执行器（Execute）
```
order-order/src/main/java/com/kyx/order/execute/impl/
├── WorkOrderExecuteImpl.java         # 售后工单执行器（核心）
├── WorkAfterSaleExecuteImpl.java     # 售后类别执行器
├── WorkFindShopExecuteImpl.java      # 找点工单执行器
├── WorkShopEditExecuteImpl.java      # 门店编辑执行器
├── WorkComplaintExecuteImpl.java     # 投诉工单执行器
└── WorkCommonExecuteImpl.java        # 通用工单执行器
```

### 服务层（Service）
```
order-order/src/main/java/com/kyx/order/service/
├── IWorkService.java
├── IWorkOrderService.java
├── IWorkOrderProblemService.java
├── IWorkFindShopService.java
└── IWorkShopEditService.java

order-order/src/main/java/com/kyx/order/service/impl/
├── WorkServiceImpl.java
├── WorkOrderServiceImpl.java
├── WorkOrderProblemServiceImpl.java
├── WorkFindShopServiceImpl.java
└── WorkShopEditServiceImpl.java

order-system/src/main/java/com/kyx/system/service/
└── IPendingWorkOrderService.java
```

### 控制器（Controller）
```
order-admin-api/src/main/java/com/kyx/admin/controller/
├── WorkController.java
├── WorkOrderController.java
└── WorkOrderProblemController.java
```

### 定时任务（Task）
```
order-admin-api/src/main/java/com/kyx/task/
└── WorkOrderTask.java            # 工单超时检查定时任务
```

---

## 十一、总结

### 工单分类维度

系统采用**多维度分类架构**：

1. **基础核心**：`t_work` 表存储所有工单的基础信息
2. **功能扩展**：根据 `type` 字段关联到不同的扩展表
   - 售后工单 → `t_work_order`
   - 找点工单 → `t_work_find_shop`
   - 门店编辑 → `t_work_shop_edit`
   - 通用工单 → `t_work_common`
   - 投诉工单 → `t_work_complaint`
3. **执行处理**：通过 `WorkTypeEnum` 的 `execute` 字段动态关联不同的执行器
4. **权限控制**：通过 `type` 分类和业务方法（`isCwType`/`isPdType`等）进行权限隔离
5. **时效管理**：通过 `WorkTimeOutType` 配置不同 `type` 的超时时间，支持3级紧急程度

### 快速查找指南

| 需求 | 查看文件 |
|-----|---------|
| 查看所有工单类型 | `WorkTypeEnum.java` |
| 查看工单状态流转 | `WorkStateEnum.java` |
| 查看超时配置 | `WorkTimeOutType.java` |
| 查看工单基础字段 | `Work.java` |
| 查看售后工单字段 | `WorkOrder.java` |
| 查看售后工单处理逻辑 | `WorkOrderExecuteImpl.java` |
| 查看超时检查逻辑 | `WorkOrderTask.java` |
| 查看工单API接口 | `WorkController.java` |

### 关键判断代码示例

```java
// 判断工单类型
if (WorkTypeEnum.afterSaleTypes().contains(work.getType())) {
    // 售后工单（41个子类型）
} else if (WorkTypeEnum.isPdType(work.getType())) {
    // 派单工单（5个子类型）
} else if (WorkTypeEnum.isCwType(work.getType())) {
    // 财务工单（2个子类型）
}

// 判断紧急程度
if (work.getLevel() == 3) {
    // 紧急工单
} else if (work.getLevel() == 2) {
    // 中等工单
} else {
    // 普通工单（level=1）
}

// 判断工单状态
switch (work.getState()) {
    case WAIT: // 待处理
    case RECEIVED: // 已接单
    case PROCESSING: // 处理中
    case FINISH: // 已完结
    case COMMENT: // 已评价
    case UNDONE: // 未完成
}
```

### 设计优势

这种设计提供了：
- ✅ **高度灵活性**：新增工单类型只需添加枚举和执行器
- ✅ **可扩展性**：不同工单类型可独立扩展字段（通过扩展表）
- ✅ **易维护性**：单一职责原则，每个执行器处理特定类型工单
- ✅ **权限隔离**：通过类型判断方法清晰划分不同角色权限
- ✅ **时效管控**：统一的超时配置和检查机制

---

## 附录

### A. 工单类型完整列表

详见 `WorkTypeEnum.java` 文件，共计约 **60+ 种工单类型**。

### B. 数据库表关系图

```
t_work (工单基础表)
├── t_work_order (售后工单)
│   └── t_work_order_problem (问题记录)
├── t_work_find_shop (找点工单)
├── t_work_shop_edit (门店编辑工单)
├── t_work_common (通用工单)
└── t_work_complaint (投诉工单)

pending_work_order (待处理工单队列)
```

### C. 重要业务规则

1. **所有工单必须在 `t_work` 表创建基础记录**
2. **售后类工单需同时在 `t_work_order` 表创建扩展记录**
3. **工单超时检查由定时任务 `WorkOrderTask` 执行**
4. **工单执行器通过 `WorkTypeEnum.execute` 动态加载**
5. **工单状态流转遵循：WAIT → RECEIVED → PROCESSING → FINISH → COMMENT**

---

**文档版本**：v1.0
**生成日期**：2025-12-11
**项目路径**：/Users/liumingkang/Code/kyx/ordersys
